{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/session-start.sh\""
          }
        ]
      },
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/dashboard-autostart.sh\""
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/user-prompt-submit.sh\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/pre-tool-edit.sh\""
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/pre-tool-bash.sh\""
          }
        ]
      },
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "STDIN=$(cat); SUB=$(echo \"$STDIN\" | jq -r '.tool_input.subagent_type // \"\"' 2>/dev/null); SID=$(echo \"$STDIN\" | jq -r '.session_id // \"\"' 2>/dev/null); INPUT=$(echo \"$STDIN\" | jq -r '.tool_input.description // \"\"' 2>/dev/null | cut -c1-300); mkdir -p \"$CLAUDE_PROJECT_DIR/.claude/logs\" && jq -cn --arg ts \"$(date -Iseconds)\" --arg sid \"$SID\" --arg sub \"$SUB\" --arg inp \"$INPUT\" '{ts: $ts, session_id: $sid, event: \"agent_start\", subagent_type: $sub, input: $inp}' >> \"$CLAUDE_PROJECT_DIR/.claude/logs/workflow-events.jsonl\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "STDIN=$(cat); FILE_PATH=$(echo \"$STDIN\" | jq -r '.tool_input.file_path // \"\"' 2>/dev/null); if echo \"$FILE_PATH\" | grep -qE '\\.claude/agents/.*\\.md$'; then mkdir -p \"$CLAUDE_PROJECT_DIR/.claude/logs\" && echo \"$(date -Iseconds) $FILE_PATH (edit detected; consider running /audit-subagents)\" >> \"$CLAUDE_PROJECT_DIR/.claude/logs/subagent-edits.log\"; fi"
          }
        ]
      },
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "STDIN=$(cat); SUB=$(echo \"$STDIN\" | jq -r '.tool_input.subagent_type // \"\"' 2>/dev/null); SID=$(echo \"$STDIN\" | jq -r '.session_id // \"\"' 2>/dev/null); INPUT=$(echo \"$STDIN\" | jq -r '.tool_input.description // \"\"' 2>/dev/null | cut -c1-300); OUTPUT=$(echo \"$STDIN\" | jq -r '.tool_response // \"\" | if type == \"object\" then tostring else . end' 2>/dev/null | cut -c1-300); mkdir -p \"$CLAUDE_PROJECT_DIR/.claude/logs\" && jq -cn --arg ts \"$(date -Iseconds)\" --arg sid \"$SID\" --arg sub \"$SUB\" --arg inp \"$INPUT\" --arg out \"$OUTPUT\" '{ts: $ts, session_id: $sid, event: \"agent_complete\", subagent_type: $sub, input: $inp, output: $out}' >> \"$CLAUDE_PROJECT_DIR/.claude/logs/workflow-events.jsonl\""
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "STDIN=$(cat); CMD=$(echo \"$STDIN\" | jq -r '.tool_input.command // \"\"' 2>/dev/null | cut -c1-200); SID=$(echo \"$STDIN\" | jq -r '.session_id // \"\"' 2>/dev/null); mkdir -p \"$CLAUDE_PROJECT_DIR/.claude/logs\" && jq -cn --arg ts \"$(date -Iseconds)\" --arg sid \"$SID\" --arg cmd \"$CMD\" '{ts: $ts, session_id: $sid, event: \"bash_complete\", command: $cmd}' >> \"$CLAUDE_PROJECT_DIR/.claude/logs/workflow-events.jsonl\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "mkdir -p \"$CLAUDE_PROJECT_DIR/.claude/logs\" && jq -cn --arg ts \"$(date -Iseconds)\" --arg sid \"${CLAUDE_CODE_SESSION_ID:-}\" '{ts: $ts, session_id: $sid, event: \"session_stop\"}' >> \"$CLAUDE_PROJECT_DIR/.claude/logs/workflow-events.jsonl\""
          },
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/stop-reviewer-gate.sh\""
          }
        ]
      }
    ]
  }
}
