# tools/secrets-allowlist.txt — reviewed allowlist for the secrets gate
# (ADR-0068 D2).
#
# NEVER-LIST RULE (per ADR-0068 D2): a secret that reaches a commit is ROTATED
# immediately — adding a real secret to this allowlist is the named anti-pattern.
# Entries here MUST be documented false positives (test fixtures, pedagogical
# examples, URL patterns, variable names, etc.).
#
# Format: one pattern per line (grep -E regex; case-sensitive).
# Blank lines and lines starting with '#' are ignored.
# Each entry MUST have a one-line reason comment directly above it.
#
# Reviewed by: implementer (slice #818, 2026-06-12). Each entry verified
# to be a false-positive (no real secret committed).
#
# --- ENTRIES ---

# dashboard/fixtures/agent-payload-sample.json: contains a test/fixture
# session_id field that looks like a token prefix but is a demo payload.
"session_id":\s*"demo-

# tools/ci-checks.sh CHECK 8 fixture assertion: the literal string
# "tool_input.subagent_type" in a grep pattern is not a secret pattern.
tool_input\.subagent_type

# decisions/*.md and CLAUDE.md: ADR prose mentions "private-key" and "token"
# as example secret types in discussion text (not actual secrets).
# The allowlist check exempts ADR/docs files by path (see secrets-gate logic).
